server.port=10010
spring.application.name=mall-product

# Apollo
app.id=${spring.application.name}
spring.config.import=apollo://datasource, apollo://application, apollo://TEST1.nacos
apollo.bootstrap.eagerLoad.enabled=true


